The recent **OpenSSL** update (specifically the transition to/optimization of the **3.5.x** branch) has impacted several packages on your system. Below is the breakdown of dependencies, changelog mentions, and a cross-distribution comparison.

### 1. Local Dependencies
The following packages on your system depend directly on `openssl` (or `openssl-3`):
*   **System Tools:** `sdbootutil`, `pesign-obs-integration`
*   **Language Runtimes/Modules:** `python313-certifi`, `lua54-luarocks`, `luajit-luarocks`, `perl-Crypt-SSLeay`
*   **Services/Applications:** `xorg-x11-Xvnc`, `openssl-3` (the base package)

### 2. Changelog Mentions of the Update
Several packages updated in the last two weeks explicitly mention OpenSSL changes:
*   **`python313-certifi` (2026-05-19):** Updated to add a missing BuildRequirement for `openssl` to correctly handle the `/etc/ssl/ca-bundle.pem` generation.
*   **`openssl-3` (2026-05-12):** The OpenSUSE update introduced **POWER performance enhancements** and optimized **ML-DSA NTT** (Module-Lattice-Based Digital Signature Algorithm).
*   **`python313-cryptography` (2026-05-09):** Although not a direct dependency, its recent update added support for post-quantum algorithms (**ML-KEM** and **ML-DSA**) specifically when used with OpenSSL 3.5.0 or later.
*   **`nginx` (2026-05-13):** Mentioned updating the OpenSSL version used for builds and fixing various SSL-related logging and protocol issues.

### 3. Cross-Distro Status Comparison
The current state of OpenSSL across the major distributions shows that while all are moving toward the 3.5 series, the timing of their most recent "major" updates differs:

| Distro | Version | Latest Update | Key Focus of Recent Changes |
| :--- | :--- | :--- | :--- |
| **OpenSUSE** | `3.5.3+` | 2026-05-12 | Performance optimizations for POWER architectures; post-quantum crypto (ML-KEM, ML-DSA). |
| **Fedora** | `3.5.6-1` | 2026-04-19 | Earlier adoption of the 3.5.x branch; focus on general stability and security bugfixes. |
| **Ubuntu** | `3.5.5-1` | 2026-04-07 | Transitioned to 3.5.x in early April; maintains both 3.5.x (Noble) and 3.0.x (Jammy) branches. |

### Summary
The update you likely received "last week" was the **OpenSUSE optimization pass** for OpenSSL 3.5, which focused heavily on **Post-Quantum Cryptography (PQC)** support and hardware-specific optimizations. While Fedora and Ubuntu updated to 3.5.x slightly earlier, your local system's dependent packages (like `python313-certifi`) have only recently been adjusted to align with these changes. The primary impact is improved performance on specific CPUs and readiness for future cryptographic standards.
