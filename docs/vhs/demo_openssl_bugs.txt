I have analysed the current status of **openssl** and **libopenssl3** across Bugzilla, openQA, and recent changelogs.

### 1. Bugzilla Bugs
Most recent bugs for `openssl` are **RESOLVED**, including critical side-channel vulnerabilities (Minerva) and FIPS integration issues. However, there are notable open items:
*   **bsc#1259271 (NEW):** A QE-filed bug for `libopenssl3` reporting that the **openQA test `ulp_openssl` fails** due to a dependency mismatch (`libopenssl3` requirement cannot be provided).
*   **bsc#760296 (NEW):** A long-standing feature request to implement `EMPTY_FRAGMENTS` for TLSv1 as a BEAST workaround.
*   **bsc#1242046 (RESOLVED):** Recently fixed issue where openQA reported an "invalid algorithm found" in FIPS mode for OpenSSL 3.5.

### 2. openQA Test Coverage
*   **openssl:** Has **26 active tests** recorded, including:
    *   **FIPS validation:** `openssl_fips_alglist`, `openssl_fips_cipher`, `openssl_fips_hash`.
    *   **Modern Cryptography:** `openssl_pqc` (Post-Quantum Cryptography), `openssl_alpn`.
    *   **Service Integration:** Tests for `nginx`, `389ds_server`, `rsyslog`, and `openvpn`.
*   **libopenssl3:** Currently has **0 recorded tests** in the mapping database, despite being flagged by `find-untested-changes` for having 14 recent changelog entries.

### 3. Recent Changelog Entries (May 2026)
The package is highly active, with the most recent entries including:
*   **2026-05-12:** POWER performance enhancements and optimized **MLDSA NTT** (Module-Lattice-Based Digital Signature Algorithm).
*   **2026-03-25:** A major security roll-up fixing **6 CVEs** (including CVE-2026-28387 and CVE-2026-31790) related to NULL pointer dereferences and heap buffer overflows.
*   **2026-01-29:** Refinement of `ulp-macros` (User-level Protocol) which links to the failing `ulp_openssl` test.

### 4. QA Triage Summary
*   **Coverage Gap:** While the `openssl` binary package is well-covered, the **`libopenssl3` subpackage represents a triage gap**. It is modified frequently but lacks formal test mapping in the catalog.
*   **Active Regression:** The open bug **bsc#1259271** indicates a current breakage in the `ulp_openssl` test suite, specifically affecting the `x86-64-v3` architecture.
*   **Feature Verification:** Several new features (MLDSA, optimized NTT) were added in May 2026 but do not yet have corresponding named tests in the `get-test-coverage` output, suggesting they are either covered by generic suites or represent new coverage requirements.

**Recommendation:** Update the TestCatalog mapping to associate `ulp_openssl` and related library-level tests with `libopenssl3` to clear the "untested" flag and track the resolution of the pending dependency failure.
